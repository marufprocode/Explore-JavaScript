var fristName = 'Jhon';
var lastName = 'Kim';

/* var fullName = fristName + lastName;
console.log (fullName); */

// but if we want make a space between name we should wirte like this 
var fullName = fristName + ' ' + lastName;
console.log (fullName);
