var apple = 11;
var person = 2;

// We will use / to get quotient/vagfol and % to get vagsesh/remainder 

var quotient= apple / person;
var remainder = apple % person;
console.log(quotient);
console.log(remainder);
